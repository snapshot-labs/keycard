describe('POST / { method: generateKey }', () => {
  describe('when the user is whitelisted', () => {
    it.todo('update and return the key');
  });

  describe('when the user is not whitelisted', () => {
    it.todo('returns a 401 code');
  });
});
